'use client';

import Map, {
  GoogleMap,
  GoogleMapsContext,
  MapConfig,
  useGoogleMap
} from './GoogleMap';

export { GoogleMap, GoogleMapsContext, useGoogleMap };
export type { MapConfig };
export default Map;
