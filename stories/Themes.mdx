import { Meta, ColorPalette, ColorItem, Markdown } from '@storybook/addon-docs/blocks';

import { colors } from '../ui/theme.ts';

<Meta title="Foundations ⚡️ / Colors 🌈 / Themes" />

# Themes

### Red

<ColorPalette>
  <ColorItem
    title="Light 🌕"
    subtitle={`var(--theme-red-*)`}
    colors={{
      Foreground: `${colors.lightRed}`,
      Background: `${colors.washedRed}`
    }}
  />
  <ColorItem
    title="Dark 🌑"
    subtitle={`var(--theme-red-*)`}
    colors={{
      Foreground: `${colors.red}`,
      Background: `${colors.maroon}`
    }}
  />
</ColorPalette>

### Orange

<ColorPalette>
  <ColorItem
    title="Light 🌕"
    subtitle={`var(--theme-orange-*)`}
    colors={{
      Foreground: `${colors.orange}`,
      Background: `${colors.washedYellow}`
    }}
  />
  <ColorItem
    title="Dark 🌑"
    subtitle={`var(--theme-orange-*)`}
    colors={{
      Foreground: `${colors.tangerine}`,
      Background: `${colors.rust}`
    }}
  />
</ColorPalette>

### Yellow

<ColorPalette>
  <ColorItem
    title="Light 🌕"
    subtitle={`var(--theme-yellow-*)`}
    colors={{
      Foreground: `${colors.yellow}`,
      Background: `${colors.lightYellow}`
    }}
  />
  <ColorItem
    title="Dark 🌑"
    subtitle={`var(--theme-yellow-*)`}
    colors={{
      Foreground: `${colors.lightYellow}`,
      Background: `${colors.brown}`
    }}
  />
</ColorPalette>

### Green

<ColorPalette>
  <ColorItem
    title="Light 🌕"
    subtitle={`var(--theme-green-*)`}
    colors={{
      Foreground: `${colors.green}`,
      Background: 'var(--color-washed-green)'
    }}
  />
  <ColorItem
    title="Dark 🌑"
    subtitle={`var(--theme-green-*)`}
    colors={{
      Foreground: `${colors.lightGreen}`,
      Background: `${colors.darkGreen}`
    }}
  />
</ColorPalette>

### Teal

<ColorPalette>
  <ColorItem
    title="Light 🌕"
    subtitle={`var(--theme-teal-*)`}
    colors={{
      Foreground: `${colors.teal}`,
      Background: `${colors.washedGreen}`
    }}
  />
  <ColorItem
    title="Dark 🌑"
    subtitle={`var(--theme-teal-*)`}
    colors={{
      Foreground: `${colors.lightestBlue}`,
      Background: `${colors.teal}`
    }}
  />
</ColorPalette>

### Blue

<ColorPalette>
  <ColorItem
    title="Light 🌕"
    subtitle={`var(--theme-blue-*)`}
    colors={{
      Foreground: `${colors.darkBlue}`,
      Background: `${colors.lightestBlue}`
    }}
  />
  <ColorItem
    title="Dark 🌑"
    subtitle={`var(--theme-blue-*)`}
    colors={{
      Foreground: `${colors.aqua}`,
      Background: `${colors.navy}`
    }}
  />
</ColorPalette>

### Purple

<ColorPalette>
  <ColorItem
    title="Light 🌕"
    subtitle={`var(--theme-indigo-*)`}
    colors={{
      Foreground: `${colors.purple}`,
      Background: `${colors.lightestPurple}`
    }}
  />
  <ColorItem
    title="Dark 🌑"
    subtitle={`var(--theme-indigo-*)`}
    colors={{
      Foreground: `${colors.lightPurple}`,
      Background: `${colors.navy}`
    }}
  />
</ColorPalette>

### Violet

<ColorPalette>
  <ColorItem
    title="Light 🌕"
    subtitle={`var(--theme-violet-*)`}
    colors={{
      Foreground: `${colors.darkPink}`,
      Background: `${colors.lightestPink}`
    }}
  />
  <ColorItem
    title="Dark 🌑"
    subtitle={`var(--theme-violet-*)`}
    colors={{
      Foreground: `${colors.pink}`,
      Background: `${colors.maroon}`
    }}
  />
</ColorPalette>
